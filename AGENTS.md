# AGENTS

## 文字コード

- このリポジトリで新規作成・更新するテキストファイルは `UTF-8 (BOMなし)` を標準とする。
- 特別な理由がない限り、`UTF-8 with BOM`、`Shift_JIS`、`EUC-JP` など他の文字コードは使用しない。
- 既存ファイルを保存し直す場合も、可能な限り `UTF-8 (BOMなし)` に統一する。

## 補足

- Windows 環境でも、文字コードの標準は `UTF-8 (BOMなし)` とする。
- エディタやツールの既定設定も、可能であれば `UTF-8 (BOMなし)` に合わせる。
